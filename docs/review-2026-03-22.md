# Code Review -- 2026-03-22

**Reviewers**: Security, Infrastructure, Code Quality
**Files reviewed**: 11 across 2 projects (claude-chat, sponsored)
**Commits covered**: 6 (3 today in claude-chat, 3 yesterday in sponsored)

---

## Critical (Act Now)

| # | Finding | Source | Location |
|---|---------|--------|----------|
| 1 | **Container runs as root** -- no `USER` directive in Dockerfile. The app only needs uid 1000 to access the tmux socket. | Infra | `claude-chat/Dockerfile` |
| 2 | **No auth on claude-chat API** -- anyone on LAN can read/send to all Claude sessions, kill them, upload files. `send_to_session` gives arbitrary prompt injection to a Claude Code process with full filesystem access. | Security | `claude-chat/app.py` |
| 3 | **No auth on SPONSORED sync endpoints** -- `POST /api/sync/*` triggers heavy FEC/Congress ETL with no auth. Could exhaust the 1000 req/hr FEC API limit. | Security | `sponsored/app/main.py` |
| 4 | **PostgreSQL exposed on 0.0.0.0:5433** -- combined with hardcoded fallback password `sponsored_pwd_change_me` in `config.py`, this is direct DB access from LAN. | Security | `sponsored/docker-compose.yml` |

## Warnings (Plan For Later)

| # | Finding | Source | Location |
|---|---------|--------|----------|
| 5 | No size limit on `/api/transcribe` upload -- reads entire file into memory, 256MB container limit means potential OOM | Infra | `app.py` |
| 6 | No size limit on `/api/sessions/{name}/send` text body -- unbounded string to tmux | Infra | `app.py` |
| 7 | Silent `except Exception: pass` on title write -- user gets 200 but title lost on restart if disk full | Code | `app.py:681` |
| 8 | `_is_claude_session()` calls `discover_sessions()` (subprocess) on every request -- called from poll endpoint every 1-5s per client | Code | `app.py:178` |
| 9 | `kill_session_endpoint` still sends C-c (lines 613-615) despite dismiss removing it for shared-PID safety -- same risk applies | Code | `app.py:598` |
| 10 | `scores.py` has ~80 lines of exact duplication between scored/fallback branches -- extract filter-building helper | Code | `scores.py:33-178` |
| 11 | `price_per_voter` and `receipt_card` return 200 with `{"error": ...}` instead of HTTP 404 | Code | `scores.py:334,407` |
| 12 | XSS design risk: `applyInline` uses `.innerHTML` after regex-built HTML strings -- not exploitable today but fragile | Security + Code | `app.js:567` |
| 13 | `logos.ts` partial match on short keys ("SHELL", "BP") could false-positive on employers like "SEASHELL" | Code | `logos.ts:126` |
| 14 | Upload directory has no cleanup/rotation (currently 5.3MB, will grow unbounded) | Infra | compose config |
| 15 | Hardcoded `STATE_POPS` dictionary (2024 Census estimates) will silently go stale | Code | `scores.py:272` |
| 16 | `sponsored.env` file permissions are 664 (world-readable) -- should be 600 | Security | config |

## Passed (32 checks)

- SQL injection (parameterized queries throughout sponsored)
- Command injection (tmux whitelist + session name regex + `-l` flag)
- Path traversal (realpath + allowlist validation)
- XSS in user messages (`.textContent` not `.innerHTML`)
- React auto-escaping in sponsored frontend
- Docker: no privileged mode, memory limits set, health check present, restart policy set
- CORS properly restricted on sponsored API
- Atomic file writes with lock for title persistence
- Single worker correct for in-memory state architecture
- Touch targets at 44px minimum (WCAG compliant)

## Recommendations (Priority Order)

1. **Add bearer token auth to claude-chat** -- even a simple env-var token check on all endpoints
2. **Bind sponsored PostgreSQL to localhost** -- change `"5433:5432"` to `"127.0.0.1:5433:5432"`
3. **Add auth to sponsored sync endpoints** -- gate behind API key or remove from public API
4. **Add `USER app` to claude-chat Dockerfile** -- `RUN useradd -u 1000 app && USER app`
5. **Add size limits** to transcribe and send endpoints
6. **Fix 200-with-error responses** in `price_per_voter` and `receipt_card` -- use HTTP 404
7. **Extract duplicated filter logic** in `scores.py`
8. **Add error logging** for title persistence failures
9. **Cache `discover_sessions()` with short TTL** to reduce subprocess spawning on polls
